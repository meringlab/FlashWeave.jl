# Documentation: http://docs.travis-ci.com/user/languages/julia/
language: julia
os:
  - linux
  # - osx
julia:
  # - 0.7
  - 1.0
notifications:
  email: false
addons:
  apt:
    packages:
    - hdf5-tools

# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia -e 'Pkg.clone(pwd()); Pkg.build("Cauocc"); Pkg.test("Cauocc"; coverage=true)'
after_success:
  # push coverage results to Coveralls
    - julia -e 'using Pkg; Pkg.add("Coverage"); cd(Pkg.dir("FlashWeave")); using Coverage; Coveralls.submit(process_folder())'
  # push coverage results to Codecov
  # - julia -e 'cd(Pkg.dir("FlashWeave")); Pkg.add("Coverage"); using Coverage; Codecov.submit(Codecov.process_folder())'
